# Default: auto-detect text files, normalize to LF
* text=auto eol=lf

# Windows batch files must stay CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files
*.pdf binary
*.elf binary
*.bin binary
*.hex binary
